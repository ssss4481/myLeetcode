#include <queue>
#include <vector>
#include <stack>
#include <deque>
#include <algorithm>

typedef std::pair<int, int> value_and_index;

class Solution {
public:
    long long numberOfSubarrays(std::vector<int>& nums) {
        long long ret;
        std::stack<value_and_index> st;
        st.push({nums[0], 0});



        


        
    }
};