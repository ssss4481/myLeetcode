#include <cmath>
#include <vector>
#include <string.h>

const int modulo = 1000000007;

class Solution {
public:

    int numberOfStableArrays(int zero, int one, int limit) {
        int dp[zero+1][one+1][2];
        memset(dp, 0, (zero+1)*(one+1)*2*sizeof(int));

        for(int i = 0; i <= zero; ++i){
            for(int j = 0; j <= one; ++j){



            }
        }



        return 
    }


};